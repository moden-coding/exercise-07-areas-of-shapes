#!/usr/bin/env python3

import math


def main():
    """
    Repeatedly ask the user for a shape name and its dimensions, and print
    its area, until an empty line is entered.

    Recognized shapes are "triangle" (base, height), "rectangle" (width,
    height), and "circle" (radius). An unrecognized shape name should
    print "Unknown shape!" instead of an area.

    Example session:
        Shape: triangle
        Base: 20
        Height: 5
        The area is 50.0
        Shape:
    """
    # enter you solution here


if __name__ == "__main__":
    main()
